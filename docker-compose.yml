version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: nefeskusadasi-web
    container_name: nefeskusadasi-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
